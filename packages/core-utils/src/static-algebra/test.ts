import { _sub, _add} from './static-sum-and-subtraction'


// informal test

const Test = () => {

    const c = '2'
    const inv = _sub('0',c)
    const a = _add('-1', '2') // 1
    const b = _sub(a,c) // -1
}

