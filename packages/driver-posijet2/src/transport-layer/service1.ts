
type WordAddress = number
type WordValue = number

type Service1 = {

    readonly run:
    
    readonly Set16BitsValue = (waddr: WordAddress, value: WordValue) => Reader<Env,Promise<TransactionResult>>


}