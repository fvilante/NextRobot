import * as R from 'ramda'
import { test } from './tagger'


describe('Template', () => { 


    it('test', () => {        
        const expected = test
        const result = "oi"
        expect(
            result
        ).toEqual(expected)
    })


})
